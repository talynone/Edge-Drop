{
  "name": "edge-drop",
  "version": "0.1.3",
  "description": "Edge-Drop — a clipboard manager that lives on the left edge of your screen and expands on hover.",
  "author": "Edge-Drop",
  "license": "Apache-2.0",
  "private": true,
  "main": "out/main/index.js",
  "type": "module",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "build:github": "set ELECTRON_BUILDER_7Z_FILTER=BCJ2&& npm run build && electron-builder --win nsis -c.extraMetadata.buildTarget=github",
    "build:dist": "set ELECTRON_BUILDER_7Z_FILTER=BCJ2&& npm run build && electron-builder --win nsis zip -c.extraMetadata.buildTarget=github --publish never",
    "build:store": "npm run build && electron-builder --win appx -c.extraMetadata.buildTarget=store",
    "build:msix": "npm run build && electron-builder --win appx -c.extraMetadata.buildTarget=store",
    "build:win": "npm run build:github",
    "package": "npm run build:github",
    "preview": "electron-vite preview",
    "test": "vitest run",
    "typecheck:node": "tsc --noEmit -p tsconfig.node.json --composite false",
    "typecheck:web": "tsc --noEmit -p tsconfig.web.json --composite false",
    "typecheck": "npm run typecheck:node && npm run typecheck:web"
  },
  "build": {
    "appId": "com.edgedrop.app",
    "productName": "Edge-Drop",
    "directories": {
      "output": "dist",
      "buildResources": "resources"
    },
    "files": [
      "out/**/*",
      "resources/**/*",
      "package.json"
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        },
        {
          "target": "appx",
          "arch": [
            "x64"
          ]
        }
      ],
      "icon": "resources/icon.ico"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "shortcutName": "Edge-Drop",
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "artifactName": "${productName}-${version}-${arch}-Setup.${ext}"
    },
    "appx": {
      "identityName": "Deepender.EdgeDrop",
      "publisher": "CN=55FECDC4-7144-4A41-98FC-056C003F76FD",
      "publisherDisplayName": "Deepender",
      "applicationId": "EdgeDrop"
    }
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "@types/react": "^18.2.73",
    "@types/react-dom": "^18.2.22",
    "@vitejs/plugin-react": "^4.2.1",
    "electron": "^34.2.0",
    "electron-builder": "^26.15.3",
    "electron-vite": "^2.1.0",
    "typescript": "^5.4.3",
    "vite": "^5.2.6",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "@radix-ui/react-scroll-area": "^1.2.13",
    "@radix-ui/react-separator": "^1.1.11",
    "@resvg/resvg-js": "^2.6.2",
    "framer-motion": "^11.0.24",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "svg-path-bbox": "^2.1.0",
    "zustand": "^4.5.2"
  }
}
